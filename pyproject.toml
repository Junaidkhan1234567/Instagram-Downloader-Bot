[project]
name = "instagram-bot"
version = "1.0.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "aiogram==3.1.1",
    "httpx==0.24.0",
    "python-dotenv==1.0.0",
    "aiosqlite==0.19.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
